manifest:
  version: "1.0"
  defaults:
    remote: ncs
    revision: main

  self:
    path: the-tag
    west-commands: scripts/west-commands.yml

  remotes:
    - name: ncs
      url-base: https://github.com/nrfconnect

  projects:
    - name: nrf
      repo-path: sdk-nrf
      revision: v3.2.4
      import: true
